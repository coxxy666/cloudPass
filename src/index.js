

import React from 'react';
import ReactDOM from 'react-dom';
 // Optional, create this file for styling
import App from './navBar.js';  // Use './App' to correctly import the App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
